name: Pipeline de Seguranca DevSecOps

on: [push]

jobs:
  security-check:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout do codigo
        uses: actions/checkout@v3

      - name: Simulacao de injecao de Segredo Seguro
        env:
          MEU_SEGREDO: ${{ secrets.API_TOKEN_DOMO }}
        run: echo "Segredo injetado com sucesso no pipeline de forma mascarada para proteger a telemetria do ESP32."
